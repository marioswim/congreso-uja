{% extends "base.html.twig" %}

{% block title %}   {{title_page}}  {% endblock %}


{%block body %}
<div class="container">
    {%block admin%}{%endblock%}
    <div class="row">
        {% for asis in publics %}

        <div class="col col-lg-4 col-md-6 col-sm-12 col-xs-12">
            <div class="asistente">
                <h4>{{asis.nombre}} {{asis.apellidos}}</h4>
                <div class="info-principal"> 
                    <div class="foto-asistente">
                        <a href="{{asset(asis.image)}}" ><div style="background-image:url({{asset(asis.image)}})"></div></a>
                        
                    </div>   
                    <p>{{asis.universidad}}</p>
                    <p>{{asis.cargo}}</p>   
                       
                </div>
            </div>
        </div>

        {%endfor%}
    </div>
</diV>
{% endblock %}

{% block footer %}


<div class="container">


</div>

{% endblock %}
